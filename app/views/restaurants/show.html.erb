<h2><%= @restaurant.name %></h2>
<h4>Menu Placeholder</h4>

<%= link_to("Edit Restaurant", "/restaurants/#{@restaurant.id}/edit", class: "btn btn-primary") %>
<%= link_to("Delete Restaurant", "/restaurants/#{@restaurant.id}", method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger") %>